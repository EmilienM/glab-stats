# Repositories are organized by team.
# Each entry can be a URL string or an object with options.

# Bot accounts to ignore in the UI
# List usernames that should be excluded from contributor rankings
# Examples: CI bots, automated systems, service accounts
bot_accounts:
  - gitlab-bot
  - dependabot
  - renovate
  - security-bot
  # Add more bot accounts as needed

repositories:
  vllm:
    - url: https://github.com/vllm-project/vllm
  gitlab:
    - url: https://gitlab.com/gitlab-org/gitlab
    - url: https://gitlab.com/gitlab-org/gitlab-runner
